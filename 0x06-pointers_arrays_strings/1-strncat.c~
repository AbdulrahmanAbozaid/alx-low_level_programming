#include <stdio.h>
#include "main.h"

/**
 * _strcat - wsde
 * @dest: gesdg
 * @src: setg
 * Return: char*
 */

char *_strcat(char *dest, char *src)
{
	int i = 0, j;

	while (dest[i])
		i++;
	for (j = 0; src[j]; ++j)
		dest[i++] = src[j];
	return (dest);
}
